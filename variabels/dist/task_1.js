"use strict";
const num = 15;
const str = "John";
const isBollean = true;
function add(num) {
    return num + 10;
}
function greeting(str) {
    console.log(`Hello ${str}`);
}
function check(bool) {
    if (bool) {
        return !bool;
    }
    else {
        return "already false value";
    }
}
